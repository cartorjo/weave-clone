.DS_Store
node_modules/

# Generated PNG masters are unreferenced by the site (JPG twins ship);
# kept local-only — regenerate or copy from the design pipeline
assets/generated/*.png
assets/archive/

# Production webroot, assembled by `npm start` (tools/build-dist.mjs)
dist/
